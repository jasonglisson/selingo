$j = jQuery.noConflict();  //select users for send email $j('document').ready(function(){     //select users for send email groups     $j(".email-to-users-group li").click(function(){         if(!$j(this).hasClass('selected')){             $j(this).addClass('selected');             var email = $j(this).data('email');             $j('.email-to-users-to').append('<span>'+email+'</span>');             $j('.email-to-users-to-ta').append(email+',').attr('name','email_to_users_groups');             $j(".email-to-users-users").hide('slow');         }         else{             alert('Already Selected');         }     });     //select users for send email individual user     $j(".email-to-users-users li").click(function(){         if(!$j(this).hasClass('selected')){             $j(this).addClass('selected');             var email = $j(this).data('email');             $j('.email-to-users-to').append('<span>'+email+'</span>');             $j('.email-to-users-to-ta').append(email+',').attr('name','email_to_users_indv');             $j(".email-to-users-group").hide('slow');         }         else{             alert('Already Selected');         }     });// media uploader     if ($j('.set_custom_images').length > 0) {         if ( typeof wp !== 'undefined' && wp.media && wp.media.editor) {             $j('.email-to-users-compose-section').on('click', '.set_custom_images', function(e) {                 e.preventDefault();                 var button = $j(this);                 var id = button.prev();                 wp.media.editor.send.attachment = function(props, attachment) {                     id.val(attachment.url);                 };                 wp.media.editor.open(button);                 return false;             });         }     }; });//check box at post page$j('document').ready(function(){    $j(".group_checkbox").change(function() {        if(this.checked) {            $j(this).val("true");        }        else{            $j(this).val("");        }    });});